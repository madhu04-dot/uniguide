name: uniguide
description: A RAG-based study application for JNGEC students.
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
